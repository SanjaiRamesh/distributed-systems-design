info:
  name: payInit
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/payInit
  body:
    type: json
    data: |-
      {
        "firmRootId": "firmRootId1",
        "amount": 20
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
